create table talk(id integer primary key,start_time timestamp, end_time timestamp, title text, speaker text, room text, type integer)
create tablefeedback(id integer primary key, created_on timestamp, rating: Integer
    comment text
    email text
    talk_id integer
    talk_type integer
    talk_title text
    talk_speaker text)